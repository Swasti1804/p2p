{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "name": "fullstack-app",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^16.5.0"
  }
}
